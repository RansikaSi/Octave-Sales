# Copy this file to .env.local and fill in real values for local dev.
# In Vercel, add the same keys under Project Settings -> Environment Variables
# (Production + Preview).

# Only needed if local requests to api.github.com fail with "unable to get
# local issuer certificate" (common on corporate networks with a TLS-inspecting
# proxy). Do NOT set this in Vercel — it disables cert verification.
# NODE_TLS_REJECT_UNAUTHORIZED=0

# Fine-grained GitHub PAT scoped to ONLY this repo, with
# "Contents: Read and write" permission and nothing else.
GITHUB_TOKEN=

GITHUB_OWNER=your-org-or-username
GITHUB_REPO=your-repo-name
GITHUB_BRANCH=main

# Shared passcode gating the /upload page and /api/update-data route.
UPDATE_PASSCODE=
